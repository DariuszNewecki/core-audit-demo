{
    "$schema": "META/data_contract.schema.json",
    "kind": "data_contract",
    "metadata": {
        "id": "contracts.proposal_scope",
        "title": "ProposalScope — proposal write boundary declaration",
        "version": "1.0.0",
        "authority": "constitution",
        "status": "active"
    },
    "description": "Declares what a Proposal will affect. files is the load-bearing field: IntentGuard enforces actual write boundaries against this declaration at execution time (CORE-Proposal.md §6). A non-empty files list is the runtime invariant enforced at proposal creation per ADR-026; the AST gate validates field presence only. Governed as a sub-object of Proposal (src/will/autonomy/proposal.py). Authority: ADR-056 Wave 1.",
    "governed_classes": [
        "ProposalScope"
    ],
    "required": [
        "files",
        "modules",
        "symbols",
        "policies"
    ],
    "properties": {
        "files": {
            "type": "array",
            "minItems": 1,
            "maxItems": 50,
            "description": "Repo-relative file paths this Proposal may modify. minItems=1 per ADR-026 D5 (proposal must declare what it touches). maxItems=50 per the blast-bound principle (2026-05-24 hardening sweep): a single proposal's per-execution blast is constitutionally bounded; larger scopes require this contract to be amended via ADR. Proposal.validate() mirrors the maxItems check at write time; IntentGuard enforces actual write boundaries at execution time."
        },
        "modules": {
            "type": "array",
            "description": "Python modules affected by this Proposal. Used for conflict detection via conflicts_with()."
        },
        "symbols": {
            "type": "array",
            "description": "Specific symbols (functions/classes) to be changed. Used for conflict detection via conflicts_with()."
        },
        "policies": {
            "type": "array",
            "description": "Constitutional policies referenced by this Proposal."
        }
    }
}
